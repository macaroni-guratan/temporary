extends layout

block content
  form(method="post", action="/create")
    input(type="hidden" name="_csrf" value!=csrfToken)
    div.flex.justify-center
      .mt-12.text-4xl
        label(for="createName") 名前
        input(type="text" name="createName")#createName.border-4.border-blue-500
      .text-4xl
        button(type="submit") 作成する